[project]
name = "spongebot"
version = "0.1.0"
description = "A Discord bot with SpongeBob-themed slash commands and wishlist management."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.4.0",
    "python-dotenv>=1.0.0",
    "aiohttp>=3.9.0",
]
